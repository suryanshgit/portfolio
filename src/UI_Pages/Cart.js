import { useContext } from "react";
import cartContext from './FoodItem'

const Cart=(obj)=>{
    // const {obj}=useContext(cartContext);
    console.log("Value inside the Cart is ");
    console.log(obj);
    
}

export default Cart;